import Page from "../components/Page";
import PageForm from '../components/PageForm'

export const meta = {
    title: "Eiendomsrettadvokater | Kontakt",
    description: "Kontakt"
}

export default ({ children }) => <Page meta={meta}>{children}</Page>

# Kontakt

Det er bare hyggelig dersom du kontakter oss hvis du har spørsmål du lurer på. Vi tar alle henvendelser seriøst og vil komme tilbake til deg så fort som mulig. Ha en fin dag videre.

<PageForm />
